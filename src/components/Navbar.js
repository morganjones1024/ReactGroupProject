 /* import {Link} from 'react-router-dom'

function Navbar(props) {
    return (
      <div className="nav">
        { <Link to="/about">About</Link> }
      </div>
    );
  }

export default Navbar; */ 